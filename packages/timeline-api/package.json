{
  "name": "timeline-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "build": "tsc",
    "clean": "rm -rf ./build",
    "start": "node build/index.js",
    "deploy": "git push -f heroku master",
    "watch": "nodemon --ext js,ts,json --watch . --exec ts-node ./src/main.ts",
    "backup": "node -r dotenv/config scripts/backup-db.js"
  },
  "dependencies": {
    "koa": "^2.13.1"
  },
  "devDependencies": {
    "@types/koa": "^2.13.1",
    "@types/node": "^15.3.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
